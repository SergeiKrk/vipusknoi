---
const { color = "standart", title = "" }: BadgeProps = Astro.props;

interface BadgeProps {
	color?: "red" | "standart";
	title?: string;
}

const colors = {
	red: "bg-brand-red/5 text-brand-red ring-1 ring-brand-red",
	standart: "bg-[#277eb5]/5 text-[#277eb5] ring-1 ring-[#277eb5]",
	white: "bg-white",
};
---

<span class={`inline-flex not-prose items-center mb-4 rounded-full px-3 py-1 text-base font-semibold ${colors[color]}`}
	>{title}</span
>
